- project:
    name: gerrit
    jobs:
      - 'mirror-git-url':
          project_pattern: https://gerrit-review.googlesource.com/p/gerrit.git

- project:
    name: git-upstream
    jobs:
      - 'mirror-openstack':
          project_pattern: openstack/git-upstream
